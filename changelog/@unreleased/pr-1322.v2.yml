type: improvement
improvement:
  description: Allow `gradle-consitent-version-idea-plugin` requirement configuration
    via `idea-configuration` plugin
  links:
  - https://github.com/palantir/gradle-consistent-versions/pull/1322
