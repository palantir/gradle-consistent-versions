type: improvement
improvement:
  description: A new `writeVersionsLocks` task that allows writing out the `versions.lock`
    without running `--write-locks` and possibly triggering expensive code paths from
    other gradle plugins.
  links:
  - https://github.com/palantir/gradle-consistent-versions/pull/723
